# תיעוד מפורט - פרויקט RafaelProject AttackDisplay

## סקירת פרויקט
הפרויקט הוא יישום סייבר המשלב ניתוח קבצים דרך VirusTotal עם מסגרת MITRE ATT&CK לסיוע בזיהוי והבנת מתקפות סייבר. היישום כולל שלושה רכיבים עיקריים:
1. **יישום אינטרנט (WEB)** - להצגת וניהול נתוני תקיפות וקבצים
2. **סייבר-בוט** - ממשק צ'ט לביצוע חקירות ופקודות מהירות
3. **מערכת חקירות (Sandbox)** - סימולציה של סנדבוקס לניתוח קבצים, עם יכולת להרחבה למערכת אמתית

## ארכיטקטורת מערכת
האתר עוקב אחר ארכיטקטורת לקוח-שרת עם שלושה רכיבים עיקריים:

**FrontEnd - React**
- נבנה באמצעות React עם הוקים (useState, useEffect) לניהול מצב
- משתמש ב-lucide-react לאיקונים מודרניים וקלי משקל
- מתקשר עם הבקאנד דרך RESTful API 
- כולל עיצוב רספונסיבי עם פאנלים צדדיים לרשימת משימות ואזור גרירה ושחרור לקבצים

**BackEnd - Node.js**
- שרת שנכתב ב-Node.js באמצעות Express (server.mjs)
- מטפל בקצות API עבור:
  - ניתוח קבצים (בדיקת hashים מול VirusTotal) - `/api/sandbox/check-file`
  - קבלת משימות sandbox - `/api/sandbox/tasks`
  - קבלת דוחות מפורטים של משימות - `/api/sandbox/tasks/:id/report`
  - חיפוש תקיפות MITRE - `/api/mitre/search`
  - פרטי תקיפות MITRE - `/api/mitre/attack/:id`
  - צ'אט בוט - `/api/bot/chat`
  - סטטיסטיקות MITRE - מוטמע בבוט ובאפשרות להרחבה לנקודת קצה נפרדת

**DataBase - sqlite**
- מאחסן נתוני תקיפות MITRE בטבלה `attacks` (מקור: קבצי JSON של MITRE ATT&CK enterprise-attack)
- מאחסן נתונים על קשרי מתקפות בטבלה `relationships`
- מאחסן דוחות sandbox/bot בטבלה `sandbox_reports` (כולל תוצאות VirusTotal ומשימות סימולציה)

## פרטי יישום לפי שלבי התרגיל

### שלב 1 – פיתוח WEB
**מטרה:** בניית יישום אינטרנט שיעזור לאנשי המודיעין בזיהוי המתקפות הפוטנציאליות והצגת דרך ההתגוננות.

**מימוש בפרויקט:**
- **הצגת מתקפות:** 
  - הערוץ הרשי להצגת נתוני MITRE ATT&CK הוא דרך ה-CyberBot (פקודות `search`, `find`) ועמודי API של הבקאנד (`/api/mitre/search`, `/api/mitre/attack/:id`).
  - בקדמי לא נבנה עמוד Grid נפרד להצגת כל הטכניקות (בגלל דגש על הפונקציונליות של הבוט וניתוח קבצים), אך כל הנתונים זמינים דרך API והבוט. can easily extend to a dedicated React page.
  
- **חיפוש לפי מילות מפתח:** 
  - פקודת הבוט: `search <keyword>` או `find <keyword>` (למשל: `search DNS`)
  - מבוצעת באמצעות נקודת הקצה `/api/mitre/search` שמקבלת פרמטרים: `q` (מילת חיפוש), `platform`, `tactic`, `page`, `limit`.
  - בחלק הקדמי, בחלק של הבוט (לאחר יישום ממשק צ'ט מתקדם) ניתן לשלב תיבת חיפש שפועלת מול API זה.

- **בונוס 1 – אתר מעוצב, חדשני, חשיבה מחוץ לקופסא ופיצ'רים נוספים:**
  - עיצוב רספונסיבי עם פאנלים מחולקים (שמאלי: רשימת משימות, ימני: דוח מפורט)
  - אזור גרירה ושחרור מותאם אישית עם משוב חזותי (צבע שינוי כאשר קובץ מעל האזור)
  - הצגת סוגי משימות שונים (בדיקת VirusTotal מול סימולציה סנדבוקס) עם סמלים וצבעים שונים
  - טעינה דינמית של משימות ודוחות עם אינדיקטורי ספינר
  - טיפול בשגיאות עם הצגת הודעות ידידותיות למשתמש
  - פיצ'רים נוספים: 
    - הצגת ערכי ברירת מחדל מתאימים במקום "null" או "undefined"
    - זיהוי אוטומטי של סוג בדיקה (VirusTotal או סנדבוקס) דרך ניתוח תגים
    - אפשרות לרענן רשימת משימות דרך כפתור

- **בונוס 2 – הצגת קשרים בין טכניקות שונות:**
  - הטבלה `relationships` במסד הנתונים מכילה את הקשרים בין טכניקות MITRE (לדוגמה: "משתמשת ב", "ממחישה את", וכו') כפי שמסופק בקבצי המקור של MITRE.
  - כרגע לא מוצג בממשק המשתמש, אך זמין דרך שאילתה ישירה למסד הנתונים. ניתן להוסיף נקודת קצה ב-backend כמו `/api/mitre/relationships/:id` שמחזירה את כל הקשרים לטכניקה מסוימת.

**כיצד נשמרו הנתונים למסד הנתונים (שלב 1):**
- נתוני MITRE ATT&CK נטענו פעמיים בעת האתחול דרך סקריפט `backend/services/insertAttacks.js` אשר:
  1. מוריד את קבצי ה-JSON מהמאגר הרשמי של MITRE (https://github.com/mitre/cti/tree/master/enterprise-attack/attack-pattern)
  2. מחלץ לכל אובייקט טכניקה את השדות: `name`, `description`, `id` (הזהות הטכנית כמו `T1059`), `x_mitre_platforms`, `x_mitre_detection`, `phase_name`
  3. שומר כל טכניקה בטבלה `attacks` עם שדה `platforms` ו-`phase_name` שמורים כמחרוזות JSON (לשימוש נוח בקוד)
  4. שומר את הקשרים בין אובייקטים (כמו `relationships`) בטבלה נפרדת `relationships`

**כיצד נתונים נשלפים (שלב 1):**
- לצורך חיפוש: הבוט שולח בקשה ל-`GET /api/mitre/search?q=<keyword>&limit=5` ומקבל בחזרה מערך של טכניקות תואמות.
- לצורך פרטים מלאים על טכניקה ספציפית: הבוט יכול להשתמש ב-`GET /api/mitre/attack/<id>` (למרות שפונקציונליות זו זמינה בבוט דרך פקודות אחרות כרגע, היא קיימת בשרת).

**כיצד המידע מוצג למשתמש (שלב 1):**
- דרך הבוט: כאשר משתמש שולח `search DLL`, הבוט מחזיר רשימה בצורה:
  ```
  Search Result: Found matching techniques in the database:
  * T1027: Obfuscated or Stolen Files
  * T1055: Process Injection
  * ...
  ```
- במידה ויובא עמוד ייעודי להקצאת וכתוב ב-React, המידע יכול להוצג בכרטיסיות (cards) או בטבלה עם סינון ומיונים.

### שלב 2 – סייבר-בוט
**מטרה:** יצירת סייבר-בוט שיעזור לאיש המודיעין בביצוע המשימות שלו, שעובד בתצורת צ'ט ומקבל פקודות מהמשתמש.

**מימוש בפרויקט:**
- קובץ מרכזי: `backend/services/botService.js` - פונקציה `processBotMessage(message)`
- תומכת לפחות בפקודות הבאות (כפי שנדרש):
  1. **ביצוע חיפוש בDB של האתר והחזרת תוצאות:**
     - פקודות: `search <keyword>`, `find <keyword>`, `query <keyword>`
     - דוגמה: `search powershell` → מחזיר טכניקות הקשורות ל-Powershell
  2. **ביצוע שאילתות מול מאגר חיצוני (VirusTotal) והחזרת תוצאות:**
     - פקודות: `check md5 <hash>`, `check sha256 <hash>`, או פשוט להזין את ה-hash ביחד עם מילים כמו `check`, `vt`, `md5`, `sha256`, `hash`
     - דוגמה: לשלוח `check md5 44d88612fea8a8f36de82e1278abb02f` → מביא תוצאה מ-VirusTotal עבור ה-hash הזה
     - התוצאה כוללת: האם הזדוני, מספר מנועים שזיהו, ופירוט לפי סוגי מנועים (בהמשך שופר לפי דרישה)
  3. **פקודות נוספות שמומשו (מעבר לדרישת המינימום):**
     - ניתוח סנדבוקס סימולציה: 
       - `sandbox analyze <filename>` - הגשת קובץ לניתוח (סימולציה)
       - `sandbox status <task_id>` - בדיקת סטטוס משימה
       - `sandbox report <task_id>` - קבלת דוח מלא של משימה
       - `sandbox list [limit]` - רשימת משימות אחרונות
     - סטטיסטיקות MITRE: `mitre stats`
     - טכניקה אקראית: `random technique` (הוטמע לפי דרישה מאוחרת)

- **בונוס: ניתוח שפה טבעית ותמיכה פקודות נוספות**
  - הבוט משתמש בתאימות חלקית (keyword matching) ולא בניתוח שפה טבעית מלא (NLP).
  - לדוגמה, הוא מזהה פקודות על ידי בדיקה אם ההודעה מכילה מילים כמו `search`, `check`, `sandbox`, וכו' ולא על ידי ניתוח סמנטי מורכב.
  - ניתן להרחיב זאת בעתיד באמצעות ספריות כמו `natural` או שימוש במודלים של שפה, אך לצורך הדגמה והדרישות, שיטת התאמת מילות מפתח מספיקה ועובדת היטב.

**כיצד נשמרו הנתונים למסד הנתונים (שלב 2):**
- כאשר הבוט מבצע פעולה שמייצרת משימה חדשה (כגון `sandbox analyze` או בדיקת קובץ דרך VirusTotal), הוא יוצר רשומה בטבלה `sandbox_reports` כולל:
  - מזהה משימה ייחודי (למשל: `SB-1234` לסימולציה סנדבוקס, `VT-5678` לבדיקות VirusTotal)
  - שם הקובץ, גודל, hashים (MD5, SHA256)
  - סטטוס (`running` לסימולציה בתחילה, משתנה ל-`completed` בסיום; תמיד `completed` עבור VirusTotal כי זהו תהליך סינכרוני)
  - ציון (ל-VirusTotal: מספר מנועי זדוניים; לסימולציה: מתחיל ב-0 ויכול להשתנות אם יש תהליך עדכון)
  - סיכום טקסטואלי, תגים (לאחסון תוצאת VirusTotal המלאה כ-JSON לזיהוי בקדמי), רישום התנהגות, פעילות רשת, טכניקות MITRE (כל אלו מהתשובה של VirusTotal או ממקום אחסון בסימולציה)
  - חותמת זמן יצירה

**כיצד נתונים נשלפים (שלב 2):**
- הבוט שולף את הנתונים ממסד הנתונים דרך פונקציות ב-`sandboxService.js` ו-`attackService.js`:
  - `getTaskStatus(taskId)` - לשליפת סטטוס וציון של משימה
  - `getTaskReport(taskId)` - לשליפת דוח מלא (כולל שדות שמיוחסים ל-JSON שמפוענחים חזרה לאובייקטים/מערכים)
  - `searchAttacks({ queryStr: keyword, limit: 5 })` - לחיפוש בטכניקות MITRE
  - נוספות כגון `getMitreStats()`, `getAttackDetails(id)`, `listRecentTasks(limit)` וכו'

**כיצד המידע מוצג למשתמש (שלב 2):**
- הבוט מחזיר אובייקט עם שדה `response` שמכיל טקסט שמצויג למשתמש בחלון הצ'ט.
- דוגמאות לפורמטי תשובה:
  - לחיפוש: 
    ```
    Search Result: Found matching techniques in the database:
    * T1059.001: PowerShell
    * T1059.003: Windows Command Shell
    ```
  - לבדיקת VirusTotal (לאחר השיפור לפי הדרישה):
    ```
    malicious: 62
    suspicious: 0
    undetected: 6
    harmless: 0
    timeout: 1
    confirmed-timeout: 0
    failure: 2
    type-unsupported: 4
    ```
  - לטכניקה אקראית:
    ```
    Random Technique:
    ID: T1078
    Name: Valid Accounts
    Tactic: Defense Evasion, Persistence, Privilege Escalation
    Platforms: Windows, Linux, macOS, Google Workspace, Azure AD, Office 365, SaaS
    Description: Adversaries may obtain and abuse credentials of existing accounts as an alternate means of bypassing access controls...
    ```
  - לסטטוס סנדבוקס:
    ```
    Sandbox Status: Investigation for Task SB-1234 records status as "completed" with a threat score of 8/10.
    ```

### שלב 3 – חקירות
**מטרה:** לעזור לאיש המודיעין להתקין מערכת sandbox (מערכת לחקירת קבצים מומלץ לחפש פתרונות קוד פתוח ב-GITHUB או להשתמש בפתרון ענני) ולהוסיף לבוט תמיכה בפקודות API מול מערכת ה-sandbox שיצר.

**מימוש בפרויקט:**
- כרגע הפרויקט כולל **סימולציה** של סנדבוקס (לא סנדבוקס אמתי שמריץ קבצים בסביבה מבודדת). הסימולציה ממוקמת ב-
  - `backend/services/sandboxService.js` - פונקציות כמו `submitFile`, `getTaskStatus`, `getTaskReport`, `listRecentTasks`, `updateTaskResults`
  - הפונקציה `submitFile` יוצרת מזהה משימה מזויף (`SB-` + מספר אקראי) ומכניסה רשומה לטבלה `sandbox_reports` עם ערכי ברירת מחדל (גודל, hashים, סטטוס `running`, ציון 0).
  - אין תהליך רקע שמעדכן את הסטטוס והציון - זה נועד להדגמה והיה צריך להיות מחובר למערכת סנדבוקס אמתית (כגון דרך webhook או polling) כדי לעדכן את התוצאות.

**כיצד לעזור להתקנת מערכת סנדבוקס אמתית:**
1. **בחירת פתרון קוד פתוח:** מומלץ להתקין [Cuckoo Sandbox](https://cuckoosandbox.org/) - מערכת אנליזת תוכנה זמינה אוטומטית בקוד פתוח.
   - מדריך התקנה בסיסי: https://cuckoosandbox.org/install/
   - דורשת מערכת לינוקס (Ubuntu מומלץ) עם VirtualBox או VMware לבידוד סביבות ניתוח.
2. **חלופות ענניות:** 
   - שימוש ב-VirusTotal כבר משולב (כמו שמבוצע כעת) מהווה מעין "סנדבוקס ענני" כי הוא מאגד תוצאות ממספר רב של מנועים ושירותי ניתוח.
   - אפשרויות נוספות: Hybrid Analysis (יש רמה חינמית), ANY.RUN (אינטראקטיבי), Joe Sandbox (גרסה קהילתית).
3. **חיבור הבוט למערכת הסנדבוקס האמתית:**
   - יש לשנות את פונקציית `submitFile` ב-`sandboxService.js` כך שהיא תשלח את הקובץ למערכת הסנדבוקס האמתית (למשל דרך REST API של Cuckoo) במקום ליצור רשומה מזוيفة.
   - יש לשנות את פונקציית `getTaskStatus` כך שהיא תבקש את הסטטוס האמתי ממערכת הסנדבוקס (במקום לקרוא רק מהמסד המקומי).
   - יש לשנות את פונקציית `getTaskReport` כך שהיא תביא את הדוח המלא ממערכת הסנדבוקס (כולל התנהגות, רשת, זיכרון, וכו') ותשמור אותו בטבלה `sandbox_reports` לעיבוד עתידי.
   - במידה והמערכת האמתית תומכת בזה, ניתן גם להוסיף עדכון מחזורית של משימות דרך `updateTaskResults` שמקבל תוצאות מהסנדבוקס ומעדכן את הרשומה.

**בונוס: הצגת תוצאות החקירה באופן ויזואלי באפליקציה**
- כבר קיים בחלק הקדמי (`SandboxTab.jsx`) ממשק להצגת תוצאות ניתוח מפורט שיכול להתאים תוצאות מסנדבוקס אמתי:
  - אזור גרירה ושחרור להעלאת קבצים
  - רשימת משימות בצד שמאל עם מזהה, שם קובץ, סטטוס, סוג (סנדבוקס או VT), וציון
  - חלון ימני להצגת דוח מפורט כאשר בוחרים משימה, הכולל:
    - מידע על קובץ (שם, גודל, hashים)
    - סטטוס וציון
    - סיכום ניתוח (לסנדבוקס אמתי: זה יכול להיות שדה `summary`)
    - תגיות (לסנדבוקס אמתי: יכולים להיות תגים כמו "TORGET-ANTIVIRUS", "סיכון גבוה", וכו')
    - טכניקות MITRE שנמצאו (מרשימה שמגיעה מהסנדבokс)
    - יומן התנהגות (דוגמה לפעולות שרשם הסנדבוקס: יצירת קבצים, עריכת רישום, יצירת חיבורים רשת)
    - פעילות רשת (דוגמה לבקשות HTTP, חיבורים DNS, תקשורת עם שרתי פקודה ושליטה)
- כל מה שנדרש הוא להבטיח שדוח הסנדבוקס האמתי יונחה במבנה התואם לשדות שמצפים להם בקדמי (או לעדכן את הקדמי לפרש את המבנה האמתי).

## טכנולוגיות/תוכנות צד שלישי וסיבות לבחירה

| טכנולוגיה | סיבה לבחירה | חלופות שנבדקו | למה לא נבחרו |
|-----------|--------------|----------------|--------------|
| **React** | ספרייה פופולרית לבניית ממשקי משתמש עם ארכיטקטורה מבוססת רכיבים שמגבירה תחזוקה ושימוש חוזר בקוד. אקוסיסטם גדול, תמיכה קהילתית חזקה. | Vue.js, Angular, JavaScript רגיל + Vanilla JS frameworks | Vue: אקוסיסטם קטן יותר ביחס ל-React לצרכים ספציפיים של ארגונים. Angular: עומס למידה גבוה, יותר מדי לפרויקט בקנה מידה זה. JavaScript רגיל: דורש יותר כתיבת יד וניהול מצב מורכב. |
| **Express.js** | מסגרת יישום אינטרנט מינימליסטית וגמישה ל-Node.js שמספקת שכבה בסיסית לתיחום נתיבים, middleware, וניהול בקשות ללא עומס מיותר. | Koa.js, Hapi.js, Sails.js | Koa: דורשת יותר כתיבת יד עבור פונקציונליות בסיסית (לרבות טיפול בשגיאות). Hapi: יותר כבד ומכוונן לארגונים גדולים. Sails: Too much abstraction ומכוונן לישומים בזמן אמת עם WebSockets. |
| **Lucide-react** | חבילת איקונים מודרנית, קלה משקל (<10KB בgz), עיצוב עקבי, תואמת ל-React מידית, וללא צורך בהתקנה נוספת. | Font Awesome, Material-UI Icons | Font Awesome: חבילה גדולה יותר, כוללת סגנונות ישנים יותר. Material-UI: בעבר דרשה תחינה של כל מודול Material-UI כדי להשתמש באיקונים, מה שהופך אותה לכבדה יותר לצורך מצומצם זה. |
| **Axios** | ספריית הבטחות ל-HTTP שעובדת גם בדפדפן וגם ב-Node.js, עם טיפול שגיאות טוב יותר מה-fetch המובנה, יכולת יירוט (interceptors), והמרה אוטומטית של JSON. | Fetch API, jQuery.ajax | Fetch API: טיפול בשגיאות פחות אינטואיטיבי (צריך לבדוק `ok` ולזרום throw ידנית), אין יירוט מובנה. jQuery.ajax: תלוי ב-jQuery (כבדה ולא נחוצה רק לבקשות HTTP). |
| **SQLite** | מסד נתונים קל משקל ללא שרת, לא דורש תהליך נפרד או תצורה מורכבת, מספיק לפרויקט בהיקף זה עם קריאות וכתיבוע נדירים יחסית. מאפשר פיתוח מהיר וקל לגיבוי (קובץ יחיד). | PostgreSQL, MySQL, MongoDB | PostgreSQL/MySQL: דורשים התקנה והפעלת שרת, תצורה של משתמשים וסיסמאות, overkill לפרויקט הדגמה. MongoDB: NoSQL לא מתאים בצורה מושלמת למבנה הנתונים היחסי של MITRE ATT&CK (טכניקות וקשרים). |
| **VirusTotal API** | שירות מוכר בתעשייה עם מאגר הנתונים הגדול ביותר של ניתוחי קבצים, מספק שכבה חינמית לשימוש בסיסי, ומציע מידע עשיר מעבר לזיהוי זדוני (כולל התנהגות, רשת, קשרי ATT&CK, קהלודי משתמשים). | Hybrid Analysis, MetaDefender, VirusShare | Hybrid Analysis: דורש הרשאה לשימוש מלא, שכבה חינמית מוגבלת מאוד. MetaDefender: פחות נפוץ בקהילה, מאגר נתונים קטן יותר. VirusShare: מתמקד בהפצת דוגמאות זדוניות ולא בניתוח מקיף עם מנועים מרובים. |
| **Better-SQLite3 (עטיפה)** | עטיפה סינכרונית ונוחה ל-SQLite ב-Node.js שמבטיחה פעולות ללא קולבקס ועם ממשק פשוט לשאילתות. | sqlite3 (המודול המקורי) | המודול המקורי דורש שימוש בקולבקס או בהבטחות שיכולים להסביך את הקוד במקרים פשוטים, בעוד עדיפה רוצה ממשק סינכרוני לקריאות/כתיבוע פשוטות ומיידיות. |

## תשובות לשאלות נוספות

### ספרו לנו על אתגר שהיה לכם בתרגיל ואיך התמודדתם איתו
**האתגר:** חילוץ וניהול נכון של נתונים מתשובות VirusTotal. בהתחלה, שדות כגון "רישום התנהגות" ו"פעילות רשת" בטופס הדוח הראו "none" או היו ריקים מכיוון ש:
1. מבנה התשובה של VirusTotal הוא מאוד מקונן ומגוון: סוגי נתונים שונים נמצאים בחלקים שונים של התגובה בהתאם לזמינות ולסוג החבילה (למשל: מנתחים התנהגות נמצאים ב-`attributes.sandbox_vermitts` בדוח קובץ רגיל, אך לפעמים זמינים רק דרך נקודת הקצה נפרדת `/behaviours`).
2. חלק מהשדות יכולים להיות חסרים לגמרי בהתאם לסוג הקובץ ולגודל.
3. הצורך לדחוס מידע מרובה שדות למסד הנתונים ולצג אותו בצורה משמעותית למשתמש.

**התמודדות:**
- עיון מעמיק בתיעוד של VirusTotal API כדי להבין את מקורות הנתונים האפשריים לכל סוג מידע.
- יישום של גישה שכבה-שכבה (fallback) לכל סוג נתון:
  - *רישום התנהגות* (`behavior_log`):
    1. ניסיון ראשון: קריאת מ-`attributes.sandbox_vermitts` בדוח קובץ (אם קיים ומערך/אובייקט).
    2. אם ריק: קריאה לנקודת הקצה `/files/{vt_id}/behaviours` וחילוץ מהתגובה (כל אובייקט התנהגות עשוי להכיל `sandbox_vermitts` משלו או שדות ישירים כמו `category`, `result`).
    3. אם עדיין ריק: השארת מחרוזת ריקה (לא לשבור את הזרימה).
  - *פעילות רשת* (`network_activity`):
    1. ניסיון ראשון: קריאת מ-`attributes.network_traffic` (אם קיים).
    2. אם ריק: קריאה לנקודת הקצה `/files/{vt_id}/behaviours` וניתוח שדות הרשת מההתנהגויות (http_requests, dns, וכו').
    3. אם עדיין ריק: שימוש בהודעות מהשדה `attributes.crowdsourced_ids_results` כמוצא אחרון (לעיתים כללי מזהים מציינים פעילות רשת כמו DNS queries ל-domains חשודים).
    4. אם עדיין ריק: מחרוזת ריקה.
  - *טכניקות MITRE* (`mitre_techniques`):
    1. חילוץ דרך ביטוי רגולרי `/T\d{4}(?:\.\d{3})?/g` מ-`attributes.crowdsourced_ids_results` (כל אובייקט במערך זה יכול להכיל שדה `signature` שמכיל טקסט עם מזהים כמו `T1059.003`).
    2. ניגוד (`[...new Set(array)]`) כדי להסיר כפילויות.
- שימוש בערכי ברירת מחדל מתאימים (מערכים ריקים עבור `mitre_techniques` ו`tags`, מחרוזות ריקות עבור `behavior_log` ו`network_activity`) כדי למנוע שגיאות בקוד העיבוד והצגה.
- הוספת לוגים למצבי חסר כדי לאפשר אבחון בעתיד ללא פגיעה בחוויית המשתמש.

### האם נהניתם מבניית האפליקציה? מאיזה חלק יותר/פחות?
**כן, נהניתי לבנות את היישום הזה.** במיוחד נהניתי מ:
- **עבודה עם API של VirusTotal:** למדתי כיצד מתבצע ניתוח איומים במציאות דרך אגגורציה של עשרות מנועים ושירותי ניתוח, וכיצד למקד מידע ממקורות מרובים לתובנה אחת.
- **יישום פונקציונליות גרירה ושחרור:** יצירת חוויית משתמש חלקה לקבלת קבצים כוללת משוב חזותי, ניהול מצבי טעינה, וטיפול בשגיאות.
- **תכנון ממשק רספונסיבי:** הבטחת שהיישום יעבוד היטב גם במחשבים שולחניים וגם במכשירים ניידים ללא צורך בתוספות מורכבות.
- **הרכבת כל הרכיבים יחד:** צפייה כיצד הבוט, מסד הנתונים, והיישום הקדמי מתחברים כדי ליצור כלי שימושי לאנליצי אבטחה (לדוגמה: משתמש מעלה חשוד לבדיקה, מקבל תוצאה מיידית בבוט, ולאחר מכן יכול לראות דוח מפורט בחלק הקדמי).

**פחות נהניתי מ:**
- **התמודדות עם אי-התאימות במבנה תגובות VirusTotal:** כפי שתואר באתגר לעיל, הצורך לכתוב לוגיקת גיבוי מרובה עבור כל סוג נתון היה מייגע ופחית מהזמן שניתן היה להשקיע בפיצ'רים חדשים.
- **ניבוי בעיות JSX של React:** לעיתים קרובות הודעות השגיאה של JSX היו לא ברורות (למשל: "Adjacent JSX elements must be wrapped in an enclosing tag") ודרשו בדיקה מדוקדקת של הקוד כדי למצוא את האלמנט הפתוח או הסוגר החסר.
- **הגדרת סביבת פיתוח בחלונות:** בעיות עם מדיניות ביצוע של PowerShell שמנעה הרצת סקריפטים כמו `npm` דרך הטרמינל המוגדר כברירת מחדל. נדרש לשנות את מדיניות הביצוע באופן זמני או להשתמש ב-PowerShell כמנהל או ב-CMD כדי לעקוף את המגבלה.

### האם הייתם משנים משהו? שימוש בשפות אחרות? מבנה הצגה שונה?
**אם הייתי בונה מחדש את היישום הזה, הייתי שוקל את השינויים הבאים:**

1. **שימוש בשפות/טכנולוגיות:**
   - **TypeScript במקום JavaScript רגיל:** להוסיף שכבת בדיקת טיפוסים שיכולה למנוע שגיאות נפוצות (למשל: העברה של `undefined` במקום מחרוזת, או טיפול שגוי באובייקטים שצפויים להיות מערך). זה היה משפר את התחזוקה ואת הביטחון בקוד, במיוחד בפרויקט עם צוות מתפתח.
   - **Next.js במקום Create-React-App:** לקבל רינדור שרת-side (SSR) מובנה, ניתוב קבצים פשוט יותר, וביצועים טובים יותר לטעינה ראשונה (חשוב אם היישום היה צובר תנועה משמעותית).
   - **PostgreSQL או MySQL במקום SQLite:** אם היישום היה אמור להתרחב לפרויקטים מרובי משתמשים עם קריאות וכתיבוע מרובים concurrent, מסד לקוח-שרת היה מציע ניהול תחרותיות טוב יותר, גיבויים מתוזמנים, ויכולת להרחבה אופקית. אך עבור פרויקט הדגמה בקנה מידה זה, SQLite הייתה בחירה מצוינת בשל הפשטות שלה.
   - **מתודולוגיית תכנון מונחה-מבנה (Schema-first) עבור API:** שימוש בכלים כמו OpenAPI (Swagger) להגדרת חוזי API לפני הכתיבה, מה שהופך את הפיתוח הקדמי והבקאנד לסינכרוני יותר ומקטין את סיכון אי-התאימות.

2. **מבנה הצגה:**
   - **הוספת ויזואליציות (גרפים, תרשימים):** במקום רק להציג טקסט גולמי בנתונים כמו ההתנהגות ורשת, הצגת גרף של פעילות רשת לאורך זמן (אם הנתונים כוללים חותמות זמן), תרשים פאי של חלוקת מנועי VirusTotal לפי קטגוריות (זדוני, חשוד, נקי), וכו'. זה היה הופך את הנתונים לנגישים יותר למשתמש שממהר.
   - **עדכונים בזמן אמת (Real-time updates):** יישום חיבור WebSocket או שימוש בשירותים כגון Server-Sent Events (SSE) כדי לדחוף עדכונים למשתמש כאשר משנה סטטוס (למשל: כאשר ניתוח סנדבוקס מסתיים). כרגע המשתמש צריך לרענן ידנית או לבדוק סטטוס דרך פקודת `sandbox status`. חיבור בזמן אמת היה משפר מאוד את חוויית המשתמש בחקירות פעילות.
   - **אפשרות לייצוא דוחות:** הוספת כפתור לייצוא דוח הניתוח בפורמטים שונים (PDF, CSV, JSON) לאחסון או שיתוף עם צוות.
   - **תכונות חיפוש וסינון מתקדמות:** ברשימת המשימות, הוספת יכולת לסנן לפי סטטוס, סוג משימה (VT מול סנדבוקס), טווח תאריכים, וציון מינימום/מקסימום.
   - **אימות והרשאת משומר:** למרות שבפרויקט זה לא נדרש, במשDeployment אמיתי היה צורך להוסיף מערכת משתמשים (למשל: JWT או sessions) כדי להגביל גישה לנתונים רגישים ולפעולות כמו ניתוח קבצים.

**לסיכום,** עם זאת, בהתחשב במגבלות הזמן ובדרישות הספציפיות של התרגיל, היישום הנוכחי עונה על כל הדרישות המוגדרות ומספק בסיס מוצק לשיפורים עתידיים. הטכנולוגיות שנבחרו מוצלחות מאוד להקמת מוצר יכולת מינימלי (MVP) מהר, ושינויים שהוזכרו לעיל הם בעיקר לשיפור קנה מידה, תחזוקה, וחוויית משתמש במצבי שימוש אינטנסיביים יותר.

## היכן לשים צילומי מסך וסרטונים
בנספח הסופי של התיעוד, יש לכלול את האלמנטים הוויזואליים הבאים:

### צילומי מסך של היישום:
1. **מסך התחלתי:** 
   - מציג את אזור הגרירה והשחרור המרכזי עם הטקסט "גרור ושחרר קובץ לניתוח" או "לחץ כדי לבחור קובץ".
   - בצד שמאל: רשימת משימות ריקה או עם מספר משימות לדוגמה.
   - מציג את הכותרת וה סLOG של היישום (אם יש).
2. **רשימת משימות אחרי העלאת קבצים:**
   - מציג שתי-שלוש משימות בדוגמה: אחת מסוג "בדיקת VirusTotal" (מזהה מתחיל ב-`VT-`) ואחת מסוג "סימולציה סנדבוקס" (מזהה מתחיל ב-`SB-`).
   - כל משימה מציגה: שם קובץ, סטטוס (`completed` או `running`), סוג משימה, ציון (לדוגמה: `42/70` עבור VT), ומזהה משימה.
3. **דוח ניתוח מפורט עבור בדיקת VirusTotal (עם כל השדות מלאים):**
   - כותרת הדוח: מציג את מזהה המשימה (למשל: `VT-1234`), שם הקובץ, הסוג (`VT Check`), והסטטוס (`completed`).
   - קטעי הדוח:
     - *מידע על קובץ:* גודל, MD5, SHA256.
     - *תוצאת VirusTotal:* הצגת פירוט מנועים (כמו בדוגמה למעלה: `malicious: 62`, `suspicious: 0`, וכו').
     - *טכניקות MITRE שנמצאו:* רשימה של מזהים (כגון: `T1059`, `T1059.001`, `T1078`) או ההודעה "לא נמצאו טכניקות MITRE" אם הרשימה ריקה.
     - *יומן התנהגות:* דוגמה לשורות מתוך ה-behavior_log (למשל: "CuckooSample: file: created", "CuckooSample: registry: key modified").
     - *פעילות רשת:* דוגמה לשורות מתוך ה-network_activity (למשל: "HTTP GET → http://malicious-domain.com/payload.exe", "DNS A query → evil.com").
4. **דוח ניתוח מפורט עבור ניתוח סנדבוקס מסורתי (למדמה):**
   - דומה למקודם אך עם הצגת שדות שמקורם בסימולציה (כגון: סיכום טקסטואלי ידוע מראש, תגים כמו ["simulation", "test-file"]).
5. **הודעת שגיאה:**
   - לדוגמה: כאשר מנסים להעלות קובץ ריק או כאשר מתקלה בתקשורת עם VirusTotal (אם מדמים כישלון), הצגת הודעת שגיאה ברורה במשטח המשתמש (באמצעות ה-`dropError` state בחלק הקדמי).

### תיאור סרטון וידאו (עד 2 דקות):
הסרטון צריך להראות את הזרימה הטיפוסית של שימוש ביישום בשלושת השלבים, תוך הדגשה על איך זה עוזר לאיש המודיעין לאתר את המתקפה אשר קיבל בדו"ח:

- **0-10 שניות:** 
  - הפעלת היישום והסבר קצר על מטרתו: "זהו יישום לזיהוי וניתוד תקיפות סייבר שמשלב מידע מ-MITRE ATT&CK ו-VirusTotal."
  - הצגת מסך הבית עם אזור הגרירה והשחרור ורשימת המשימות הרקעית.

- **10-25 שניות:** 
  - הדגמה של העלאת קובץ חשוד: משיכת קובץ דוגמה (למשל: קובץ עם ה-hash של EICAR או דוגמית לא מזיקת שמדמה התנהגות חשודה) לאזור הגרירה והשחרור.
  - צפייה בהודעת "מעבד..." והצגת אינדיקטור טעינה בחלק העליון.

- **25-50 שניות:** 
  - לאחר סיום הניתוח, הצגת איך המשתמש בוחר במשימה החדשה מרשימת המשימות בצד שמאל.
  - החלון הימני מתעדכן ומציג דוח ניתוח מפורט. הדגשת כל ארבעת השדות הנדרשים:
    - *תגיות:* מראה את תוצאת VirusTotal בצורה פשוטה (למשל: אם זה קובץ זדוני, מראה "VT Result: Malicious (65/75 engines)" או פשוט את פירוט המנועים כפי שהוטמע בבוט).
    - *טכניקות MITRE שנמצאו:* אם נמצאו, מציג רשימה של מזהים (למשל: `T1059.001: PowerShell`, `T1055: Process Injection`) עם הסבר קצר שכל טכניקה מציינת שלב ושיטת תקיפה שונים.
    - *יומן התנהגות:* מציג דוגמה של שתי-שלוש שורות מה-behavior_log (למשל: "Created file in %TEMP%", "Modified registry key HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run").
    - *פעילות רשת:* מציג דוגמה של שתי-שלוש שורות מהפעילות הרשת (למשל: "HTTP POST to [*] /gate.php", "DNS query for c2-malicious-domain.com").
  - הקריין מסביר: "מהנתונים האלה יכולים אנשי המודיעין להסיק שהקובץ מנסה ליצור קיום במערכת דרך שינוי רישום ולהתקשר עם שרת פקודה ושליטה, מה שמתאים לטכניקות T1113 ו-T1071."

- **50-75 שניות:** 
  - הדגמה כיצד לעבור בין משימות שונות ברשימה (להציג לפחות שתי משימות עם תוצאות שונות) ולראות איך הדוח משתנה בהתאם.
  - אפשרות להצגת פקודת הבוט במקביל: כתיבת `check md5 <hash>` בקונסולת הבוט וקיבלת תגובה מיידית עם פירוט המנועים (כמו בשלב הקודם).

- **75-90 שניות:** 
  - סיכום של הפונקציות המרכזיות איך זה עוזר לאנליצי אבטחה:
    - "העלאת קובץ אחת נותנת תמונה מלאה: מי העשה את זה (באמצעות זיהוי זדוני), איך הוא עשה את זה (הנוהג ההתנהגותי והפעילות הרשתית), ואיזה טכניקות מתוך ה-framework של MITRE ATT&CK הוא השתמש בהן."
    - "זה מאפשר תגובה מהירה: חסימת ה-IP שאליו הקובץ מתקשר, הסרת המפתח הרישומי שיצר, והכוונת חיפוש אחרי דוגמאות דומות בארגון באמצעות טכניקות ה-MITRE שזוהו."

- **90-120 שניות:** 
  - מסך סיום עם הלוגו והודעה: "מפותח עבור מודיעין הסייבר - מגשימים 2026".
  - אפשרות להצגת קוד QR או קישור להורדת הקוד (אם רלוונטי).

הסרטון צריך להיות ברזולוציה קריאה (לפחות 720p), עם הקריין ברור ומוזיקת רקע עדינה מאוד (אם בכלל) כדי לא להסיח מההסבר. מומלץ להדגיש את movements של העכבר ולהשתמש בזום על אזורים חשובים במסך כאשר מדברים עליהם.