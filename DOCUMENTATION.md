# תיעוד פרויקט attackdisplay

## סקירת פרויקט
attackdisplay היא יישום אינטרנט שתוכנן כדי לעזור לאנליצי אבטחה לזיהוי וניתוח מתקפותไซבר באמצעות מסגרת MITRE ATT&CK. היישום משתלב עם VirusTotal כדי לבדוק קבצים לאיומים וכן מספק ממשק לחיפוש והצגת פרטי טכניקות תקיפה.

## ארכיטקטורת מערכת
היישום עוקב אחר ארכיטקטורת לקוח-שרת עם שלושה רכיבים עיקריים:

### frontend (React)
- נבנה באמצעות React עם הוקים לניהול מצב
- משתמש ב-lucide-react לאיקונים
- מתקשר עם הבקאנד דרך API RESTful
- כולל עיצוב רספונסיבי עם פאנלים צדדיים לרשימת משימות ותצוגת דוחות

### backend (Node.js/Express)
- שרת שנכתב ב-Node.js באמצעות Express
- מטפל בקצות API עבור:
  - ניתוח קבצים (בדיקת hashים מול VirusTotal)
  - קבלת משימות ארגז חול
  - קבלת דוחות מפורטים של משימות
  - חיפוש תקיפות MITRE
  - פרטי תקיפות MITRE
  - צ'אט בוט קיברנטי

### מסד נתונים (SQLite)
- מאחסן דוחות ארגז חול בטבלה sandbox_reports
- מאחסן נתוני תקיפות MITRE בטבלה נפרד (מנוהל על ידי שירותים נפרדים)

## פרטי יישום

### כיצד נשמרו הנתונים למסד הנתונים
הנתונים נשמרים למסד הנתונים דרך נקודת הקצה `/api/sandbox/check-file` בבקאנד. כאשר קובץ נשלח:
1. הקובץ מתקבל כממוחזר בינרי דרך middleware של express.raw
2. מחושבים hashים MD5 ו-SHA256 של הקובץ
3. ה-hash נבדק מול VirusTotal באמצעות שירות virusTotalService.js
4. התוצאות מה-VirusTotal (כולל התנהגות התנהגות, פעילות רשת וטכניקות MITRE) מאוחסנות בטבלת sandbox_reports
5. בפועל מאוחסנים שדות:
   - מזהה: מזהה משימה ייחודי (התחילית 'vt-' + מספרים אקראיים)
   - filename: שם הקובץ המקורי שנשלח
   - filesize: גודל הקובץ בבתים
   - md5: hash md5 של הקובץ
   - sha256: hash sha256 של הקובץ
   - status: תמיד מוגדר ל-'completed' עבור בדיקות VirusTotal
   - score: מספר מנועי זדוניים מ-VirusTotal
   - summary: סיכום טקסטואלי של תוצאת VirusTotal
   - tags: האובייקט המלא של VirusTotal מוצפן כמחרוזת JSON (משמש את הקדמי לזיהוי בדיקות VirusTotal)
   - behavior_log: רישום התנהגות מ-VirusTotal (או מחרוזת ריקה)
   - network_activity: נתוני פעילות רשת מ-VirusTotal (או מחרוזת ריקה)
   - mitre_techniques: מערך של מזהי טכניקות MITRE שנמצאו על ידי VirusTotal (מקודד כ-JSON)
   - created_at: חותמת זמן שעה ביצירת המשימה

הקוד האחראי לפעולה זו ניתן למצוא בבקאנד/server.mjs בפונקציית הטיפול בקצה `/api/sandbox/check-file` (שורות ~172-231).

### כיצד נתונים נשלפים
הנתונים נשלפים מהמסד הנתונים דרך שני קצוות API עיקריים:

1. `/api/sandbox/tasks` - שולף רשימה של משימות ארגז חול אחרונות
   - משתמש בפונקציה listRecentTasks מ-sandboxService.js
   - מחזיר משימות ממוינות לפי תאריך יצירה (החדישות ראשון)
   - מוגבל ל-20 משימות ברירת מחדל (ניתן להגדרה דרך פרמטר שאילתה limit)

2. `/api/sandbox/tasks/:id/report` - שולף דוח מלא עבור משימה מסוימת
   - משתמש בפונקציה getTaskReport מ-sandboxService.js
   - שולף את כל השדות עבור מזהה משימה מסוים מטבלת sandbox_reports
   - הקדמיני משתמש בזה כדי להציג דוחות ניתוח מפורטים לאחר בחירת משימה מרשימה

הקדמיני שולף את הנתונים האלה באמצעות בקשות fetch רגילות:
- loadTasks() קורא ל-`/api/sandbox/tasks?limit=20` כדי למלא את רשימת המשימות
- loadTaskReport(taskId) קורא ל-`/api/sandbox/tasks/${taskId}/report` כדי לקבל מידע מפורט על המשימה שנבחרה

### כיצד המידע מוצג למשתמש
המידע מוצג למשתמש דרך רכיב ה-React sandboxTab.jsx, שמחולק לשני חלונות עיקריים:

#### חלון שמאלי (רשימת משימות ושליטה)
- מציג רשימה של משימות ארגז חול אחרונות
- כל פריט ברשימה מציג:
  - שם קובץ
  - סטטוס (למשל completed)
  - סוג משימה (בדיקת VirusTotal מול ניתוח ארגז חול)
  - ציון (לבדיקות VirusTotal: מנועי זדוניים/סך כל המנועים)
  - מזהה משימה ייחודי
- משתמשים יכולים ללחוץ על כל משימה כדי להציג את הדוח המפורט שלה
- החלון מכיל בקרות ל:
  - רענון רשימת המשימות
  - חיפוש קבצים (כרגע מיושם כמחליף)
  - הצגת הודעות שגיאה אם מתרחשות בעיות

#### חלון ימני (אזור גרירת קבצים ותצוגת דוח)
- החלק העליון מכיל אזור גרירת קבצים שבו המשתמשים יכולים:
  - לגרור ולשחרר קבצים לניתוח
  - ללחוץ כדי לבחור קובץ דרך בוחר קבצים מסורתי
  - לראות רמזים חזותיים כאשר קובץ נגרר מעל אזור השחרור
- החלק התחתון מציג את דוח הניתוח המלא של המשימה שנבחרה:
  - כותרת הדוח המציגה את מזהה המשימה, שם הקובץ, סוג (בדיקת VirusTotal מול ניתוח ארגז חול) והסטטוס
  - קטעי דוח בהתאם לסוג המשימה:
    * עבור בדיקות VirusTotal:
      - מידע על קובץ (גודל, hashים MD5/SHA256)
      - תוצאה של VirusTotal (זדוני/נקי עם מספר זיהויים)
      - פרטי מנוע (פירוק תוצאות מנועי VirusTotal)
      - תגיות (סיכום תוצאת VirusTotal)
      - טכניקות MITRE שנמצאו (רשימה של מזהי טכניקות ATT&CK)
      - יומן התנהגות (דוגמה להתנהגויות שנצפו במהלך ניתוח)
      - פעילות רשת (דוגמה לפעילות רשת שנצפתה)
    * עבור ניתוחי ארגז חול (פונקציונליות מקורית):
      - מידע על קובץ
      - סיכום ניתוח
      - תגיות (תוויות טקסט פשוטות)
      - טכניקות MITRE שנמצאו (מניתוח ארגז חול)
      - יומן התנהגות
      - פעילות רשת

הרכיב משתמש בזיווקי React (useState, useEffect) לניהול מצב:
- tasks: רשימת המשימות האחרונות
- selectedTask: המשימה המוצגת כרגע
- loading/reportLoading: דגלים המציינים קריאות API מתמשכות
- dropAreaActive: דגל המציין כאשר קובץ נגרר מעל אזור ההשלכה
- dropError: מאחסן כל הודעות שגיאה להצגה למשתמש

### טכנולוגיות/תוכנות של צד שלישי שנמשכו וסיבות לבחירתן

1. React
   - סיבה לבחירה: ספרייה פופולרית לבניית ממשקי משתמש עם ארכיטקטורה מבוססת רכיבים שהופכת את הקוד לתחזוקה יותר
   - אפשרויות שנבחנו: Vue.js, Angular, JavaScript רגיל
   - למה React: אקוסיסטם גדול, תיעוד מצוין, ביצוע טוב ליישומי עמוד יחיד

2. Express.js
   - סיבה לבחירה: מסגרת יישום אינטרנט מינימליסטית וגמישה ל-Node.js שמספקת מערך חזק של תכונות ליישומי אינטרנט וניידים
   - אפשרויות שנבחנו: Koa.js, Hapi.js, Sails.js
   - למה Express: פשטות, אקוסיסטם middleware, קבלה נרחבת בקהילת Node.js

3. Lucide-React
   - סיבה לבחירה: חבילת איקונים מודרנית תואמת ל-React שמספקת איקונים עקביים ואיכות גבוהה
   - אפשרויות שנבחנו: Font Awesome,アイקונים של Material-UI
   - למה Lucide: גודל חבילה קטן יותר, עיצוב מודרני, תמיכה ב-TypeScript מוכנה מהקופסה

4. API של VirusTotal
   - סיבה לבחירה: שירות מוכר בתעשייה שמספק ניתוח מקיף של קבצים לאיתור תוכנה זדונית באמצעות מספר מנועי אנטיוירוס
   - אפשרויות שנבחנו: Hybrid Analysis, MetaDefender, VirusShare
   - למה VirusTotal: מאגר הנתונים הגדול ביותר, שכבה חינמית לשימוש בסיסי, פונקציונליות נרחבת (התנהגותו התנהגות, רשת, מיפוי ATT&CK)

5. SQLite
   - סיבה לבחירה: מסד נתונים קל משקל ללא שרת שאינו דורש תהליך שרת נפרד
   - אפשרויות שנבחנו: PostgreSQL, MySQL, MongoDB
   - למה SQLite: פשטות עבור פרויקט בגודל זה, אין דרישות תצורה, ביצועים מספקים לעומס הצפוי

6. Axios
   - סיבה לבחירה: ספרייה המבוססת על הבטחות לבקשות HTTP שעובדת גם בדפדפן וגם ב-Node.js
   - אפשרויות שנבחנו: Fetch API, jQuery.ajax
   - למה Axios: טיפול השגיאות טוב יותר, פונקציית יירוט, המרה אוטומטית של נתוני JSON

## תשובות לשאלות נוספות

### ספרו לנו על אתגר שהיה לכם בתרגיל ואיך התמודדתם איתו
האתגר הגדול ביותר היה הוצאה נכונה והצגה של נתונים מהתשובות העשירות של VirusTotal. בהתחלה שדות הדוח כגון "תגיות", "טכניקות MITRE שנמצאו", "יומן התנהגות" ו"פעילות רשת" הראו "none" או היו ריקים, מכיוון ש:

1. מבנה תשובת VirusTotal הוא מורכב ומקונן
2. סוגי נתונים שונים נמצאים בחלקים שונים של התשובה
3. חלק מהקצוות דורשים הרשאות מיוחדות או יש להם פורמטי תשובה שונים

גברתי על האתגר על ידי:
- עיון מפורט בתיעוד של API של VirusTotal כדי להבין את מבנה התשובה
- יישום של מספר אסטרטגיות אחזור עבור כל סוג של נתונים:
  * עבור יומני התנהגות: בדקתי תחילה את ה-attributessandbox_verdicts בדוח על קובץ, עם גיבוי אל נקודת הקצה /behaviours אם זה הכרחי
  * עבור פעילות רשת: בדקתי את ה-attributenetwork_traffic, לאחר מכן את נקודת הקצה /behaviours, ולבסוף השתמשתי בהודעות כלל מזהים של crowdsourced_ids_results כמוצא אחרון
  * עבור טכניקות MITRE: השתמשתי בביטוי רגולרי לחילוץ מזהים של צורת txxxx או txxxx.yyy מהשדה crowdsourced_ids_results
- טיפול נכון במקרי קצה (כאשר הנתונים אינם זמינים מ-VirusTotal)
- הבטחת ערכי ברירת מחדל מתאימים (מערכים ריקים, מחרוזות ריקות) במקום לאפשר לשגיאות להתרחש

### האם נהניתם מבניית האפליקציה? מאיזה חלק יותר/פחות?
כן, נהניתי לבנות את היישום הזה. בפרט נהניתי מ:
- עבודה עם API של VirusTotal ולמידה לגבי הדרך שבה מתבצעת ניתוח איומים במציאות
- יישום פונקציונליות גרירה ושחרור להעלאת קבצים
- תכנון ממשק רספונסיבי שעובד היטב הן במחשבים שולחניים והן במכשירים ניידים
- צפייה כיצד כל הרכיבים מתחברים יחד כדי לספק כלי שימושי לאנליצי אבטחה

פחות נהניתי מ:
- התמודדות עם אי-התאימות במבנה תגובות VirusTotal (נקודות קצה שונות מחזירות נתונים בפורמטים שונים)
- ניבוי בעיות JSX של React, שפעמים היה להן הודעות שגיאה מבלבלות
- הגדרת סביבת פיתוח בחלונות (מדיניות ביצוע של PowerShell שגרמה לבעיות עם npm)

### האם הייתם משנים משהו? שימוש בשפות אחרות? מבנה הצגה שונה?
אם הייתי בונה מחדש את היישום הזה, הייתי שוקל את השינויים הבאים:

1. שימוש בשפות/טכנולוגיות:
   - הייתי שוקל להשתמש ב-TypeScript במקום JavaScript רגיל לבדיקת טיפוסים טובה יותר ותחזוקה מוגברת
   - הייתי שוקל להשתמש ב-Next.js במקום ב-Create-React-App לביצועים טובים יותר ו-SSR מובנה
   - הייתי שוקל להשתמש במסד נתונים כמו PostgreSQL במקום ב-SQLite לקנה מידה טוב יותר ופונקציות (אם היישום היה צומח מעבר לפרויקט הדגמה)

2. מבנה הצגה:
   - הייתי מוסיף יותר ויזואליזציות (גרפים, תרשימים) כדי להפוך את הנתונים לנגישים יותר
   - הייתי מיישם עדכונים בזמן אמת באמצעות WebSockets או אירועים שנשלחו מהשרת לחוויית משתמש חלקה יותר
   - הייתי מוסיף אפשרות לייצוא דוחות בפורמטים שונים (PDF, CSV)
   - הייתי מיישם תכונות חיפוש וסינון מתקדמות יותר לרשימת המשימות
   - הייתי מוסיף אימות והרשאת משתמש למשDeployment מאובטח יותר

עם זאת, בהתחשב בכך שזהו פרויקט הדגמה עם דרישות ספציפיות, היישום הנוכחי עונה על כל הדרישות המוגדרות ומספק בסיס מוצק לשיפורים עתידיים.

## היכן לשים צילומי מסך וסרטונים

במסמך הסופי, יש לכלול את האלמנטים הוויזואליים הבאים:

1. צילומי מסך של היישום:
   - צילום מסך שמציג את המסך ההתחלתי עם אזור הגרירה והשחרור וכן רשימת משימות ריקה
   - צילום מסך שמציג את רשימת המשימות אחרי העלאת מספר קבצים
   - צילום מסך שמציג דוח ניתוח מפורט עבור בדיקת VirusTotal (עם כל ארבעת השדות הנדרשים מלאים בנתונים נכונים)
   - צילום מסך שמציג דוח ניתוח מפורט עבור ניתוח ארגז חול מסורתי
   - צילום מסך שמציג הודעת שגיאה כאשר העלאת קובץ נכשלת

2. תיאור סרטון וידאו (עד 2 דקות):
   הסרטון צריך להראות:
   - 0-10 שניות: הפעלת היישום והסבר על מטרתו
   - 10-30 שניות: הצגת גרירה ושחרור של קובץ זדוני (למשל דוגמית EICAR) לאזור הגרירה והשחרור
   - 30-50 שניות: צפייה כיצד היישום מעבד את הקובץ ומציג הודעת "טוען..."
   - 50-1:10: הצגת דוח ניתוח מפורט עם כל ארבעת השדות הנדרשים מלאים בנתונים נכונים:
     * תגיות: מראה את תוצאת VirusTotal (זדוני/נקי)
     * טכניקות MITRE שנמצאו: מראה רשימה של מזהי טכניקות (אם זמינות) או את ההודעה "לא נמצאו טכניקות MITRE"
     * יומן התנהגות: מציג אירועי התנהגות לדוגמה מ-VirusTotal
     * פעילות רשת: מציג נתוני פעילות רשת לדוגמה מ-VirusTotal
   - 1:10-1:30: הצגת איך לעבור בין משימות שונות ברשימה
   - 1:30-2:00: סיכום של הפונקציות המרכזיות ואיך זה עוזר לאנליצי אבטחה לזהות תקיפות

תיעוד זה מכיל את כל המידע הנדרש לפי הוראות הפעילות, מסביר את החלטות העיצוב, פרטי היישום, וכן מספק הקשר לגבי למה נבחרו גישות מסוימות.